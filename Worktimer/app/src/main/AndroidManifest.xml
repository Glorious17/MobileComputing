<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pjj.worktimer.worktimer">

    <uses-permission android:name="com.android.vending.BILLING" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/zea_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/zea_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".form.Generate_Project_Form" />
        <activity android:name=".projectScreen.ProjectView" />
        <activity
            android:name=".form.Login"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".form.Register"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>